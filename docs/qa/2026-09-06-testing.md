# 2026-09-06 QA記録

## 対象と実行条件

- 作業対象: `PuzzleTools/puzzle-kit`。開始HEAD `511a926e527bba3d8f779316166c93f33523561e`。
- 開始branch `puzzle-kit-refactor` → 作業branch `feature/test-harness`。
- 復帰用tag `baseline/test-harness/20260906`。tagはHEADのみを保存し、開始時の未コミット変更は含まない。
- 既存の未コミット変更: `package.json` のyajilin-kit / tsx、`src/solver/adapters/index.ts`、未追跡のyajilinKit関連2ファイル。保持している。
- Node 25.2.1 / Vitest 4.0.16 / Playwright 1.62.0 / Vite 7.3.0 / macOS arm64。
- Chromium Desktop 1280×720、Pixel 7エミュレーション。Firebase `.env` はQAサーバーで無効化。
- テスト用ローカルViteサーバー・ブラウザーの起動には実行環境のsandbox外実行を使用。GitHubはconnectorで読み取り、投稿なし。

## 実行結果

| 検査 | Before | After |
| --- | --- | --- |
| 既存unit | 944件成功。ただしPlaywrightの2ファイルを誤収集してコマンド失敗 | includeをsrcに限定。追加integration込み945件 / 55ファイル成功 |
| #40入力フックintegration | `ReferenceError: getToolCategory is not defined` | 線確定、Undo成功 |
| Issue E2E（5シナリオ×2環境） | 3成功 / 7失敗 | 7成功 / 3失敗 |
| 既存NPGenerator E2E | 10成功 / スクリーンショット専用4 skip | 10成功（同梱XMLでも成功） |
| UI / harness E2E | 今回追加 | 10成功 / `/edit`の2失敗 |
| 全E2E（通常スイート） | 未整備 | 27成功 / 5失敗 / 4 skip、flaky 0 |
| app型チェック | 37 diagnostics | 34 diagnostics（未解決） |
| E2E専用型チェック | なし | `pnpm typecheck:e2e` 成功 |
| 追加ハーネスのESLint | なし | 成功 |
| カバレッジ | provider未導入 | 取得成功。Statements 32.05%、Branches 26.60%、Functions 31.22%、Lines 32.33% |

カバレッジ対象はutils/store/hooks/npgen。アプリ全体・同梱solver・Wasmの網羅率ではない。coverage閾値によるゲートはまだ設定していない。

[一括検査summary](../../artifacts/check/2026-09-06T00-54-01-156Z/summary.json) / [E2Eレポート](../../artifacts/check/2026-09-06T00-54-01-156Z/e2e/report/index.html) / [型チェックログ](../../artifacts/check/2026-09-06T00-54-01-156Z/typecheck.log)。この実行後にE2E専用型チェックをqa:checkの追加ステップとして組み込んだ。

最終設定でも [全シナリオを再録画](../../artifacts/qa/2026-09-06T00-59-56-285Z-after/report/index.html) し、27成功 / 5失敗を再確認した（スクリーンショット専用4件はこの実行対象に含めない）。このrunには `source-manifest.json`、成功/失敗双方の最終画面とtraceがある。

## before / after 動画

**PRで共有する証跡:** [動画4本・画面2枚・比較ページ](evidence-20260906/README.md)。以下の `artifacts/` リンクは元workspace内だけで有効。

[動画比較ページを開く](../../artifacts/qa/comparison-2026-09-06T00-51-41-511Z/index.html)

- [Before HTMLレポート](../../artifacts/qa/2026-09-06T00-47-47-499Z-before/report/index.html)
- [After HTMLレポート](../../artifacts/qa/2026-09-06T00-49-58-702Z-after/report/index.html)
- [UI調査レポート](../../artifacts/qa/2026-09-06T00-51-39-004Z-after/report/index.html)
- [動画検査結果](../../artifacts/qa/video-verification.json): before/afterの20本をffprobeで検査し、すべて正の再生時間を確認。

比較対象のIssue E2Eは同じ操作とviewport。Before時点では未修正、After時点で `getToolCategory` importと矢印キー変数参照の2箇所だけ製品コードを修正している。Afterでは成功時のtraceも保持するよう設定を拡張した。初回の `00-47-09` captureはセレクターのstrict mode違反のため比較対象外。

| シナリオ | Desktop Before → After | Pixel設定 Before → After |
| --- | --- | --- |
| #40 Free Segment / Undo / Redo | 線なし＋例外 → 成功 | 線なし → 線なし（レイアウト/操作到達の切り分けが必要） |
| #20 再ドラッグ消去 | 見た目の消去は成功、mouse up例外あり → 例外なしで成功 | 初回描画ができない → 同じ状態 |
| #19 クリック数字のBackspace | 成功 → 成功 | 初回クリックで数字なし → 同じ状態 |
| 矢印キーで数字選択を移動 | `key is not defined` → 2セルに入力成功 | 同じ例外 → 2セルに入力成功 |
| #22 数字選択後の盤面外クリック | 数字変更なし → 数字変更なし | 数字変更なし → 数字変更なし |

## 画面証跡

- [Desktop Master](../../artifacts/qa/2026-09-06T00-51-39-004Z-after/test-results/ui-audit-UI-audit-master-chromium/initial.png): 1280×720、盤面1056×548。
- [Mobile Master](../../artifacts/qa/2026-09-06T00-51-39-004Z-after/test-results/ui-audit-UI-audit-master-mobile-chrome/initial.png): viewport幅412にdocument幅560、盤面幅188。
- [Mobile Paint](../../artifacts/qa/2026-09-06T00-51-39-004Z-after/test-results/ui-audit-UI-audit-paint-mobile-chrome/initial.png): 盤面高19.906px。ツール群で画面の大部分を占有。
- [開発ハーネス](../../artifacts/qa/2026-09-06T00-51-39-004Z-after/test-results/ui-audit-UI-audit-harness-html-chromium/initial.png): シナリオ切替・Reset・JSON検査。機能E2EはPC/Pixel設定の両方で成功。

`/edit` は初期画面の描画中に `Maximum update depth exceeded`。初期画面の取得完了に至らないため、失敗時スクリーンショット・動画・runtime-errorsをレポートに記録した。

## 今回残した課題

- `/edit`の更新ループ、Master/Paintのモバイルレイアウトは未修正。再現テストを赤のまま残している。
- appの34型エラーには作業中のyajilin adapterの引数不一致2件も含まれる。ユーザーの作業を保持し、無関係な型の広範修正はしていない。
- #18 / #21 / #23はIssueを読んだ段階。全バグの再現完了とはしていない。
- 特殊tool全種類、実機touch、Safari/Firefox、playerの公開URL、外部ストレージや認証は今回の範囲外。
- 初回QA時点でGitHubのIssueはopenのまま。後続の公開作業でPRを作成するが、Issueのcloseや既存PRのmergeは行わない。

再実行手順は [テストハーネス](../testing.md) を参照。UIレビュー文書は非追跡の `.work/ui-review/` で管理する。

## PR公開用checkoutの検証

ヤジリンの未コミット変更を除外し、PRに含めるファイルだけを `/private/tmp` の独立checkoutへ配置して検証した。npm ci成功、E2E型チェック成功、ハーネスの限定ESLint成功。E2Eは27成功 / 5失敗 / 4skipで、元workspaceと同じ5件が失敗した。app型チェックは32 diagnostics（元workspaceの34からヤジリン由来2件を除いたもの）。

単独checkoutで初めて判明したPenpa parityテストの外部フォルダ参照は、上流のテーブルをライセンス付きfixtureとして固定して解消した。unitは55ファイル・945件すべて成功した。既存PRとの重複はPR本文に記載した。
