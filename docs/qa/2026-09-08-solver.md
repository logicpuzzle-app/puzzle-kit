# Solver integration QA — 2026-09-08 JST

Implementation: `e9a5034cba88934a3a76ba7f7909dd6ee4257170` on `feature/pr-review-followup`, PR #55. Tests use Playwright with real browser workers and the committed Wasm; no external solver service or Computer Use is required.

## Results

| Check | Result |
| --- | --- |
| Application and E2E type checks | Passed |
| Unit | 1,030 passed, 66 files |
| Full E2E, desktop/mobile Chromium and WebKit | 224 passed, 8 existing recording-only skips, 0 failed/flaky |
| Production build | Passed |
| Production desktop/mobile Chromium | 30 passed, 0 skipped/failed/flaky |
| New solver scenarios | 8 scenarios × 4 browser profiles; all passed |

The production suite includes 16 solver checks, plus the existing 14 editor/persistence/NPGenerator checks. The obsolete optional solver export warning is removed. Existing chunk-cycle/size warnings and generated solver sourcemaps referencing absent upstream sources remain.

## Reproduction and comparison

[Open the video comparison](evidence-solver-20260908/index.html). Download/open the HTML locally to play the videos; GitHub's source viewer does not run it. [Metadata](evidence-solver-20260908/metadata.json) records source hashes, test results, video durations, and SHA-256 checksums. All 24 videos were fully decoded with FFmpeg and checked for nonzero duration.

| Scenario | Before Chromium | After Chromium / mobile Chromium |
| --- | --- | --- |
| Nurikabe unique answer, then impossible clues | Passed | Passed / passed |
| First Wasm download fails, then Solve again | Retry remains failed | Passed / passed |
| Multiple solutions with confirmed cells | Partial-result panel without overlay | Passed / passed |
| Bundled Heyawake with canonical room walls | Wrong empty shaded-cell set | Correct black cell `(0,1)` / correct |
| Cancel loading, then Solve with a fresh worker | Passed | Passed / passed |
| Toolbar URL import, then Solve | Imported genre not enabled; no Solve button | Passed / passed |
| Worker startup throws, then retry | Failure panel absent | Passed / passed |
| Slitherlink unique answer | Passed, 30 loop segments | Passed / passed |

Before: `artifacts/qa/2026-09-07T16-38-20-706Z-before`, baseline app `785729d`, **5 reproduced failures and 3 passing controls**. The capture's working-tree patch and source manifest are preserved. NPGenerator cancellation was already modified in that capture but does not participate in these solver scenarios. The baseline test source extracted from the trace is also included.

After: `artifacts/check/2026-09-07T16-45-14-185Z/e2e`, committed implementation `e9a5034`. The final driver closes the result panel before retrying on narrow screens and uses its Cancel button. Earlier exploratory runs with incomplete dialog selectors or unsuitable puzzle fixtures are not used as product-failure evidence.

The default Solve UI prefers cspuz. The Heyawake case explicitly starts the real bundled solver worker, sends an imported puzzle, and asserts its response; production uses the hashed worker asset. Compare the accompanying before/after **worker-result JSON** for this case. Its video records the imported board; it does not inject that worker's answer into the UI or imply that the default backend changed. Both URL import entry points now share genre selection and grid/topology setup.

NPGenerator's already-aborted signal and queued progress after cancellation are **unit-test reproductions**, not claimed as UI-video reproductions. The existing production NPGenerator case also confirms successful generation through the actual Wasm worker. The before unit logs are included alongside the evidence.

## Re-run

```sh
npm ci
npx playwright install chromium webkit
npm run qa:check
npm run build
npm run qa:production
npm run qa:capture -- after e2e/solver.spec.ts --project=chromium --project=mobile-chrome
```

GitHub CI repeats clean installation, all checks, production build/QA, and uploads recordings and traces for 30 days. See [PR #55 checks](https://github.com/logicpuzzle-app/puzzle-kit/pull/55/checks) for the final pushed revision. The repository includes the comparison videos so their availability does not depend on artifact retention.

Coverage is application integration for representative genres (Nurikabe, Slitherlink, Heyawake), worker failures/cancellation and NPGenerator boundaries. This does not establish exhaustive correctness for every generated solver or all puzzle sizes. Design/UI review notes remain ignored under `.work/`.
