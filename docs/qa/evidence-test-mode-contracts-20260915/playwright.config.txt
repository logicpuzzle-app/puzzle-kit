import config from '../playwright.config';
export default {
  ...config, testDir: '.', testMatch: 'mode-capture.spec.ts', testIgnore: [],
  projects: config.projects!.filter(p => ['chromium', 'mobile-chrome'].includes(p.name!)).map(p => ({ name: p.name, use: p.use })),
};
